name: Verified source release
on:
  workflow_run:
    workflows: ["Baseline Quality", "PressWarden independent validation", "Database Quality", "Detection Quality", "Finding History Quality", "PHP Detection Quality", "Progress Quality", "Quarantine Quality", "Report Quality", "Run State Quality", "Update Quality"]
    types: [completed]
    branches: [main]
permissions:
  contents: read
concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false
jobs:
  publish:
    if: >-
      github.repository == 'marketania/PressWarden' &&
      github.event.workflow_run.head_repository.full_name == github.repository &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      actions: read
    steps:
      - name: Check out only the trusted main push candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          persist-credentials: false
      - name: Verify all exact-commit main checks, package source and publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 .github/scripts/release.py
